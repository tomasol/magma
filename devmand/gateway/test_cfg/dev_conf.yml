devices:
  - id: localhost
    ip: 127.0.0.1
    type: Device
    platform: PlaintextCli
    yangConfig: localhost.json
    channels:
      cliChannel:
        port: 22
        username: ubnt
        password: ubnt
        stateCommand: show mac access-lists